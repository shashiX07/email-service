# ========================================
# EMAIL API SERVICE - ENVIRONMENT VARIABLES
# ========================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ========================================
# SERVER CONFIGURATION
# ========================================
# Port for local development (Vercel uses its own port in production)
PORT=3001

# API Key for authentication - CHANGE THIS to a strong, unique value
# Generate a secure key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY=your_super_secret_api_key_change_this_immediately

# Environment: development | production
NODE_ENV=development

# ========================================
# GMAIL SMTP CONFIGURATION
# ========================================
# Gmail SMTP server settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# Your Gmail credentials
# IMPORTANT: Use a Gmail App Password, NOT your regular password
# How to get App Password:
# 1. Enable 2-Factor Authentication on your Google Account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Select "Mail" and generate a 16-character password
# 4. Use that password here (remove spaces)
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password

# ========================================
# DEFAULT EMAIL SETTINGS
# ========================================
# These are used as defaults for the /api/send-email endpoint
# when from/fromName are not specified in the request

# Default sender email (should match SMTP_USER)
DEFAULT_FROM_EMAIL=your-email@gmail.com

# Default sender display name
DEFAULT_FROM_NAME=Your Name or Company Name

# Default recipient for contact form submissions
DEFAULT_TO_EMAIL=recipient@gmail.com

# ========================================
# OPTIONAL: ALTERNATIVE SMTP PROVIDERS
# ========================================
# Uncomment and configure if using a different email provider

# SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your_sendgrid_api_key

# Mailgun
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_mailgun_smtp_username
# SMTP_PASS=your_mailgun_smtp_password

# AWS SES
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_ses_smtp_username
# SMTP_PASS=your_ses_smtp_password

# Outlook/Office 365
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your_outlook_password

# ========================================
# SETUP CHECKLIST
# ========================================
# [ ] Copy this file to .env
# [ ] Change API_KEY to a secure random string
# [ ] Enable 2FA on your Gmail account
# [ ] Generate Gmail App Password
# [ ] Fill in SMTP_USER with your Gmail address
# [ ] Fill in SMTP_PASS with your App Password
# [ ] Set DEFAULT_FROM_EMAIL (same as SMTP_USER)
# [ ] Set DEFAULT_FROM_NAME to your preferred name
# [ ] Set DEFAULT_TO_EMAIL to where you want contact forms sent
# [ ] Test with: npm run dev
# [ ] Verify with: curl http://localhost:3001/health

# ========================================
# SECURITY NOTES
# ========================================
# - Never commit .env to version control
# - Use different API keys for dev/staging/production
# - Rotate API keys periodically
# - Keep App Passwords secure
# - Use strong, unique passwords
# - Review access logs regularly
